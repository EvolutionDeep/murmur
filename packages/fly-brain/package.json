{
  "name": "@fly/fly-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic ~1,080-neuron LIF connectome, motor decoder and market stimuli for the murmur fly agents — pure TypeScript, with no native or WASM dependency.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "npx --yes tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
