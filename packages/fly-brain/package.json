{
  "name": "@fly/fly-brain",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic LIF connectome (1,080 neurons by default; production sizes the species spec to 30,800 via BRAIN_N_*, while each live bred fly is genome-sized ~10,800), motor decoder and market stimuli for the murmur fly agents — pure TypeScript, with no native or WASM dependency.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "npx --yes tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
