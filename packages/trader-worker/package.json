{
  "name": "@fly/trader-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object runtime for murmur: reads Arc market temperature, drives the fly population, archives ticks to D1, and settles the agent economy in real USDC over x402.",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "npx --yes tsx --test src/*.test.ts",
    "compile:registry": "node scripts/compile-registry.mjs",
    "deploy:registry": "node scripts/deploy-registry-auto.mjs",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@fly/fly-brain": "*",
    "viem": "^2.21.45"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "solc": "^0.8.37",
    "typescript": "^5.6.3",
    "wrangler": "^4.133.0"
  }
}
